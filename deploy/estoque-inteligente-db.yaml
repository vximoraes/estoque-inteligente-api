apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: estoque-inteligente-mongodb
  name: vol-estoque-inteligente-mongodb
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1000Mi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    io.kompose.service: estoque-inteligente-mongodb
  name: estoque-inteligente-mongodb
spec:
  replicas: 1
  selector:
    matchLabels:
      io.kompose.service: estoque-inteligente-mongodb
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        io.kompose.service: estoque-inteligente-mongodb
    spec:
      containers:
        - image: mongo:8
          livenessProbe:
            exec:
              command:
                - mongosh
                - --eval
                - db.adminCommand('ping')
            failureThreshold: 5
            periodSeconds: 10
            timeoutSeconds: 5
          name: estoque-inteligente-mongodb
          ports:
            - containerPort: 27017
              protocol: TCP
          envFrom:
            - configMapRef:
                name: estoque-inteligente-env
          volumeMounts:
            - mountPath: /data/db
              name: vol-estoque-inteligente-mongodb
      restartPolicy: Always
      volumes:
        - name: vol-estoque-inteligente-mongodb
          persistentVolumeClaim:
            claimName: vol-estoque-inteligente-mongodb
---
apiVersion: v1
kind: Service
metadata:
  labels:
    io.kompose.service: estoque-inteligente-mongodb
  name: estoque-inteligente-mongodb
spec:
  type: ClusterIP
  ports:
    - name: '27017'
      port: 27017
      targetPort: 27017
  selector:
    io.kompose.service: estoque-inteligente-mongodb
